// for vai executar varias vezes um mesmo comando, bloco de codigo ou declaraçao que vamos fazer dentro do for

// iterar = passar vez por vez dentro de um laço

// condicao = expressao que será avaliada a cada iteração, se o resultado dessa expressao for true a declaração vai ser executada

// anatomia do for:
// ------for(inicializador; condicao; incremento){
//         declaracao
//        }
for(let i = 1; i < 6; i++){
  console.log(i)
}

// a cada iteracao vamos executar o +1